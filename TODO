- Complete Phong lighting ray-casting
- Redo Makefile
- Object to delete its geometry?
- Light (Point, Dir, Spot), Primitives (Sphere, Plane), Intersection
- Texture class (Procedural and noise(wood, marble, checkerboard, clouds, ...))
- Add Texture component to Material
- Refraction
- Adaptive Subsampling
  - if the same object, then compute directly from its attributes, else find
  - create more interesting scene once everything works
- Testing, measuring
